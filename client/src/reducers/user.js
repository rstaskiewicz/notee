const initialState = {
    avatar: '',
    name: '',
    email: '',
    authenticated: false
}

export default (state = initialState, { type, payload }) => {

    switch (type) {

        default:
            return state

    }

}
